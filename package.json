{
  "name": "games-reporting",
  "version": "1.0.0",
  "private": true,
  "description": "Crash and error reports from Ruben's games, taken over HTTP and kept in SQLite.",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test \"test/**/*.test.mjs\""
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0"
  }
}
